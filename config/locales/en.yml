# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  request:
    bad_request: "Invalid request syntax" #400 :bad_request
    unauthorized: "User is not authenticated" #401 :unauthorized
    forbidden: "Cannot access the resource requested, the user does not have access to it." #403 :forbidden
    not_found: "Requested resource cannot be found" #404
    bad_request: "Bad request." #400

  activerecord:
    attributes:
      task:
        status: "Task status"
    errors:
      models:
        task:
          attributes:
            status:
              inclusion: "is invalid: %{value}."